{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "description": "Sample server for Sub-Organization home pages",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joshua Tanner",
  "dependencies": {
    "express": "4.14.0"
  }
}
